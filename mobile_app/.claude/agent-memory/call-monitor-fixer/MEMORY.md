# Memory Index

> 🚨 **動任何前端或後端的「視訊通話／來電通知／監控（CCTV）」程式碼之前，
> 必須先完整閱讀 `D:\114project\Uban\CLAUDE_call-monitor.md`。**
>
> 那是本子系統的**唯一權威參考**（前後端合併，約 1130 行）：
> §2 檔案地圖與風險等級／§3 Socket.IO・FCM・SharedPreferences 資料契約／
> §4 通話生命週期（含冷啟動五層兜底）／**§5 UI 按鈕與畫面跳轉地圖**／
> §6 監控子系統／**§7 護欄 G1–G36（絕對不可單點修改）**／§8 十四輪修復年表／
> §9 驗證與除錯（三層 A/B/C 定位法）／§10 修改 SOP。
>
> 這條鏈路經過 14 輪真機故障追查，每一層看似多餘的防護都對應一次實際故障。
> **單點修改幾乎必然造成回歸。** 文件與程式碼衝突時以**程式碼**為準，
> 並回頭更新該文件的 §7.3「已知的文件錯誤」。
>
> 後端根目錄是 `uban-api/`，不是舊文件寫的 `uban-api/uban-api/`。
> `Uban/mobile_app/lib/main.dart.bak` 是過期備份，會污染 grep，永遠不要編輯。

（尚無其他記憶。新增時每則一行：`- [標題](檔名.md) — 一句話重點`。）
