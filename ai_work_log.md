# Uban AI 開發工作日誌 (AI Work Log)

這是由 AI 自動維護的開發日誌，用於紀錄專案進度、解決的問題與未完成事項，方便在不同設備間共享進度。

---

## 📅 2026-04-20

### ✅ 已完成工作 (Completed Work)

1.  **長輩首頁 (ElderHomeTab) UI 瘦身與調整**：
    *   **字體優化**：縮小「頭條早知道」字體 (36/44 -> 28/34)，並將其字體更換為與「代誌報給你知」相同的 **StarPanda**。
    *   **佈局優化**：縮減「代誌報給你知」與「好友」功能區塊的高度 (220/260 -> 175/215)，減少下滑才看得到新聞的困擾。
    *   **間距優化**：減少了日曆卡片 (Calendar Card) 的字體與內邊距，並縮減全域 SizedBox 間距，使首頁在不滑動的情況下能顯示更多新聞內容。
    *   **細節調整**：縮小了「最新」旁邊的紅點提示以及「好友」面板內的字體。

2.  **新聞播放器 (NewsListenPlayer) 修正**：
    *   **字幕回歸**：修正了在播放「預錄新聞」(含有 `audio_url`) 時，`_subtitles` 會被強行歸零的 Bug，現在預錄新聞也能正確顯示同步動態字幕。

3.  **小豬遊戲：2D 卡通桌寵進化 (Enhanced 2D Desktop Pet)**：
    *   **風格轉型**：捨棄先前的 3D 擬真風，改為精緻的 **2D 向量卡通風格**（粗輪廓、扁平化色塊），更貼近 Mobile App 的簡約介面且對長輩更具辨識度。
    *   **真正幀動畫**：實作了 **Walk Cycle (行走幀)**。小豬在首頁散步時，會切換兩張行走圖，呈現真實的「邁步」動作。
    *   **多狀態切換**：根據 `idle` (待機)、`walking` (走路)、`sleeping` (睡覺)、`happy` (互動快樂) 狀態，動態切換不同的 2D 繪畫（例如睡覺會閉眼、快樂會大笑）。
    *   **洪水填充去背**：採用精準的 Flood-fill 演算去背，修復了「眼睛被挖空」的 Bug，保持眼神靈動。
    *   **全域畫風統一**：同步更新了排行榜與個人檔案中的小豬圖像，確保全 App 體驗一致。
    *   **高性能導航**：點擊首頁動態小豬可直接跳轉至 `LeaderboardScreen`。

### ⚠️ 遇到的問題 (Issues Encountered)

*   **UI 空間不足**：長輩端 UI 規範要求字體不得小於 24pt，但在小螢幕手機上容易造成排版擠壓，目前的解法是透過縮減不必要的內邊距 (Padding) 與中間間隔 (SizedBox) 來平衡空間。
*   **預錄字幕同步**：原本的播放器邏輯漏掉了 pre-generated 模式下的字幕載入，已補上資料映射。

### ⏳ 待辦事項/未來計畫 (Pending/Plan)

*   [ ] **持續更新此日誌**：每次對專案進行重大改動後都需更新此文件。
*   [ ] **驗證跨設備同步**：請使用者確認不同設備間是否能看到此檔案的更新。
*   [ ] **效能觀察**：觀察縮減 UI 尺寸後，首頁在不同解析度裝置上的兼容性。

---
